---
import { generation, pokemonNumbers } from '../core/constants';

const randomPokemon = Math.floor(Math.random() * pokemonNumbers[generation]) + 1;

return Astro.redirect(`/pokemon/${randomPokemon}`);
---
